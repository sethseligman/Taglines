/** Inline stacks — use next/font variables from `app/layout.tsx` */
export const FONT_PLAYFAIR = 'var(--font-playfair), Georgia, "Times New Roman", serif';
export const FONT_DM = 'var(--font-dm-sans), ui-sans-serif, system-ui, sans-serif';
